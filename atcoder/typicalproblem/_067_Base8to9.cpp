/* 067 Base 8 to 9 */

#include <iostream> // 標準ライブラリ
#include <cmath> // pow() is included in it.
using namespace std; // 名前空間

void sumation(int number; int &count) {
    // 与えられる整数Nをnumberに代入する。（アドレスで参照）
    // 9進法に変換して、8を5に変換した数値を返す。
}
int main(void) {
    int n, k;
    int count = 0; // 累乗した数を足していくための変数 count
}
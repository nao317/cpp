// マインスイーパー

#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int h, w;
    cin >> h >> w;

    vector<string> str(h);

    for (int i = 0; i < h; i++)
    {
        cin >> str[i];
    }

    vector<vector<int>> vec(h)(w);

    for (int i = 0; i < h; i++)
    {
        for (int j = 0; j < w; j++)
        {
            
        }
    }
}